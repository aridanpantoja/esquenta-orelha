---
trigger: always_on
---

- Always use 'type' instead of 'interface'
- For page components (like in page.tsx), always use 'export default function'
- For normal components, always use 'export function'
